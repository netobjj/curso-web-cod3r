const [a, b, c, d] = [1, 3, 5, 8] // Destructuring: forma reduzida de criar 4 variáveis

const soma = a + b + c + d
const subtracao = a - d
const multiplicacao = a * d
const divisao = a / d
const modulo = b % 2 // Resto da divição: usado para saber se o número é ímpar ou par

console.log(soma, subtracao, multiplicacao, divisao, modulo)
